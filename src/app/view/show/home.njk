{% extends "show/layout.njk" %}

{% block content %} 
<div class="info">
    <div class="content">
        <h1>As melhores receitas</h1>
        <p>aprenda a construit os melhores pratos com 
            receitas criadas por profissionais do
            mundo inteiro
        </p>
    </div>
    <img src="/chef.png" alt="Chef">
</div>

<section>
    <h2>Mais acessadas</h2>
    <div class="most-accessed">

        {% for item in items %}
        <div class="card" index="{{item.index}}">
            <img src="{{item.image}}" alt="{{item.title}}">
            <h2>{{item.title}}</h2>
            <h3>{{item.author}}</h3>
        </div>
        {% endfor %}

    </div>
</section>
{% endblock %}

{% block script %}
<script src="/scriptsShow.js"></script>
{% endblock %}